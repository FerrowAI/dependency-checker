# Dependency Checker

Scan dependencies for vulnerabilities & updates. Ferrow security scanning.

```javascript
const checker = new DependencyChecker();
const report = await checker.check(packageJson);
```

Features: Vuln scanning, update detection, Ferrow security.
License: MIT
